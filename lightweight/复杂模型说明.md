se-resnet101 参数量：125.9270133972168 KB

参数文件大是由于当时联邦学习把两个复杂模型的都保存了，实际很大部分是多余的。

bottleneck堆叠层数：

    SEResNet1D(SEBottleneck1D, [3, 4, 23, 3], **kwargs)

se-resnet101总结构：

    x = self.conv1(x)
    x = self.bn1(x)
    x = self.relu(x)
    x = self.maxpool(x)

    x = self.layer1(x) # 64ch
    x = self.layer2(x) # 128ch
    x = self.layer3(x) # 256ch
    x = self.layer4(x) # 512ch

    x = self.avgpool(x)
    x = x.flatten(1)
    x = self.dropout(x)
    x = self.fc(x)

bottleneck结构：

    out = self.conv1(x)
    out = self.bn1(out)
    out = self.relu(out)

    out = self.conv2(out)
    out = self.bn2(out)
    out = self.relu(out)

    out = self.conv3(out)
    out = self.bn3(out)
    out = self.se(out) # 加SE模块

    out = self.drop_path(out)
    out += identity
    out = self.relu(out)

se结构：

    class SELayer1D(nn.Module):
        def __init__(self, channel, reduction=16):
            super(SELayer1D, self).__init__()
            self.avg_pool = nn.AdaptiveAvgPool1d(1)
            self.fc = nn.Sequential(
                nn.Linear(channel, channel // reduction, bias=False),
                nn.ReLU(inplace=True),
                nn.Linear(channel // reduction, channel, bias=False),
                nn.Sigmoid()
            )
    
        def forward(self, x):
            b, c, _ = x.size()
            y = self.avg_pool(x).view(b, c)
            y = self.fc(y).view(b, c, 1)
            return x * y.expand_as(x)
    
    def conv1x3_1d(in_planes, out_planes, stride=1):
        return nn.Conv1d(
            in_planes, out_planes, kernel_size=3,
            stride=stride, padding=1, bias=False
        )

测试集平均准确率：
    
    Class 1 91.51
    Class 2 96.54 
    Class 3 81.59

提供了三类小样本量数据集（从测试数据集抽样）在dataset文件夹中。

训练采用联邦学习，两个复杂模型进行联邦训练，训练超参数：

    "epochs": [50],
    "batch_size": [512],
    "lr": [1.6e-1],
    "weight_decay": [1e-4],
    "optimizer": ["SGD"], 
    "dropout_rate": [0.2],
    "momentum": [0.9],

优化器，采用onecycle策略训练设置：

    lr_max = cfg["lr"]
    init_lr = lr_max / 15.0

    if cfg["optimizer"] == "Adam":
        optimizer_a = Adam(model_a.parameters(), lr=init_lr, weight_decay=cfg["weight_decay"])  
        optimizer_b = Adam(model_b.parameters(), lr=init_lr, weight_decay=cfg["weight_decay"])  
    else:
        optimizer_a = SGD(model_a.parameters(), lr=init_lr, momentum=cfg["momentum"], weight_decay=cfg["weight_decay"])  
        optimizer_b = SGD(model_b.parameters(), lr=init_lr, momentum=cfg["momentum"], weight_decay=cfg["weight_decay"])  

    grad_accum_steps = int(cfg.get("grad_accum_steps", 1))
    grad_accum_steps = max(1, grad_accum_steps)
    steps_per_epoch_a = math.ceil(len(train_loader_a) / grad_accum_steps)  
    steps_per_epoch_b = math.ceil(len(train_loader_b) / grad_accum_steps)  

    scheduler_a = OneCycleLR(  
        optimizer_a,
        max_lr=lr_max,
        epochs=cfg["epochs"],
        steps_per_epoch=steps_per_epoch_a,
        pct_start=0.4,
        final_div_factor=5e2,
        anneal_strategy="cos",
        cycle_momentum=True
    )  

